# ─── Frontend (.env.example) ───────────────────────────────────────────────
# Copie este arquivo para .env e preencha com a URL do seu servidor

# Desenvolvimento local (padrão — não precisa mudar):
# VITE_WS_URL=ws://localhost:4000

# Produção — cole aqui a URL do Railway ou Render:
VITE_WS_URL=wss://SEU-SERVIDOR.railway.app
